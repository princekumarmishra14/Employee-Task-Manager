/**
 * Delay values for network latency simulation (in ms)
 */
export const API_DELAY = {
  MIN: 600,
  MAX: 1400,
};

/**
 * Configure true to simulate API server failures globally for QA testing.
 */
export const SIMULATE_FAILURE = false;
